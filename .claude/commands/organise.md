Perform maintenance on the answers directory:

1. **Group by topic**: Review all files in `answers/`. Group related answers into topic subfolders (e.g., `answers/apis/`, `answers/databases/`, `answers/security/`). Use clear, broad categories. Don't over-fragment - aim for 3-8 items per folder minimum.

2. **Update index**: Create or update `answers/INDEX.md` listing all answers organised by topic folder, with the question summary and link to each answer file.

3. Report what was reorganised.
