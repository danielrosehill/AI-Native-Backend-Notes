The user will provide one or more questions (possibly dictated, so clean up any transcription artifacts).

For each question:
1. Clean up the text for clarity while preserving the original intent
2. Save it as a separate markdown file in `questions/to-run/` with a descriptive kebab-case filename (e.g., `best-api-for-weather-data.md`)

Report back how many questions were saved and their filenames.

User input: $ARGUMENTS
