We currently have a common practice of defining APIs and then creating separate routes for MCP tools on top of them. In an AI-native backend, we might want to unify the development of APIs and MCP tools so we can visualize exactly which routes are exposed as API endpoints, which are exposed as MCP tools, and which are both. What tooling exists today for unifying API and MCP tool development with this kind of visibility?
